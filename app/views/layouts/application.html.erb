<!DOCTYPE html>
<html>
  <head>
    <title>UrlShortener</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;700&display=swap" rel="stylesheet">
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div class="relative h-screen flex flex-col items-center justify-center">
      <div class="p-8 md:p-16 shadow-2xl flex flex-col items-center justify-center">
        <div class="text-4xl md:text-6xl lg:text-8xl mb-2 md:mb-8 font-bold" >URL Shortener</div>
        <% flash.each do |type, msg| %>
          <div data-controller="clipboard" class="mb-2 md:mb-8 flex flex-col bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative" role="alert">
            <strong class="font-bold m-auto">This is the short url!</strong>
            <%= link_to msg, id: "short_link_url", class: "flex justify-center" do %>
              <span data-clipboard-target="source" class="block underline sm:inline"><%= msg %></span>
            <% end %>
            <button data-action="clipboard#copy" class="flex items-center justify-center">
              <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-6 h-6">
                <path stroke-linecap="round" stroke-linejoin="round" d="M15.666 3.888A2.25 2.25 0 0013.5 2.25h-3c-1.03 0-1.9.693-2.166 1.638m7.332 0c.055.194.084.4.084.612v0a.75.75 0 01-.75.75H9a.75.75 0 01-.75-.75v0c0-.212.03-.418.084-.612m7.332 0c.646.049 1.288.11 1.927.184 1.1.128 1.907 1.077 1.907 2.185V19.5a2.25 2.25 0 01-2.25 2.25H6.75A2.25 2.25 0 014.5 19.5V6.257c0-1.108.806-2.057 1.907-2.185a48.208 48.208 0 011.927-.184" />
              </svg>
              Copy to Clipboard
            </button>
          </div>
        <% end %>

        <div class="w-full">
          <%= yield %>
        </div>
      </div>
      <div class="absolute right-0 bottom-4 w-full flex justify-center">
        <span class="text-md font-thin italic">
          Made with with love by <a class="font-bold" href="https://twitter.com/pmareke">@pmareke</a>
        </span>
      </div>
    </div>
  </body>
</html>
